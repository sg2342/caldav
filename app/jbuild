(executables
 ((names (caldav_server))
  (libraries (checkseum.c digestif.c irmin-unix logs logs.fmt caldav webmachine nocrypto lwt ptime ptime.clock.os cohttp-lwt-unix mirage-kv-lwt rresult mirage-clock-unix irmin-mirage io-page-unix mirage-conduit tcpip tcpip.stack-socket))))

(install
 ((section bin)
  (files ((caldav_server.exe as caldav_server)))))
